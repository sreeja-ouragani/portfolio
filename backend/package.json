{
  "name": "aws-lambda-nodemailer",
  "version": "1.0.0",
  "description": "AWS Lambda function for sending emails using Nodemailer",
  "main": "lambda.js",
  "dependencies": {
    "dotenv": "^16.4.7",
    "nodemailer": "^6.10.0"
  }
}
